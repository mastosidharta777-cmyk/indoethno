# Deploy the same codebase as two separate Vercel projects.
# indoethno.com
SITE_MODE=coming-soon

# pilot.indoethno.com
# SITE_MODE=pilot

# Required only when inquiry persistence is enabled.
# Keep these server-side. A publishable key is deliberately used with RLS.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
